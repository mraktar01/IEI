[build]
  # Leave this completely blank so Netlify knows not to look for package.json
  command = ""
  
  # A single dot means "publish exactly what is in my main folder"
  publish = "." 

  # This tells Netlify where your backend login script lives
  functions = "netlify/functions"
